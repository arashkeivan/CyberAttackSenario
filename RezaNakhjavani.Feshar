اسکریپت آزمایشی شبیه‌سازی دسترسی به Telegram CLI از طریق ROM دستکاری شده

هشدار بسیار مهم: این اسکریپت صرفاً برای آموزش، تست نفوذ در محیط آزمایشگاهی مجاز و تحقیقات امنیتی طراحی شده است. استفاده غیرقانونی از آن ممنوع است.

۱. اسکریپت استخراج اطلاعات Telegram از دستگاه Android (محیط آزمایشی)

```python
#!/usr/bin/env python3
# telegram_forensic_extractor.py
# شبیه‌سازی استخراج اطلاعات Telegram از یک دستگاه Android با ROM دستکاری شده
# (فقط برای اهداف آموزشی و تست نفوذ مجاز)

import os
import sqlite3
import json
import hashlib
from datetime import datetime
import shutil

class TelegramForensicExtractor:
    def __init__(self, device_path="/mnt/forensic_extract"):
        """
        شبیه‌سازی استخراج از دستگاه Android با ROM مخرب
        device_path: مسیر شبیه‌سازی شده دسترسی به حافظه دستگاه
        """
        self.device_path = device_path
        self.output_dir = "telegram_forensic_report"
        os.makedirs(self.output_dir, exist_ok=True)
        
        # مسیرهای احتمالی داده‌های Telegram در Android
        self.telegram_paths = [
            "/data/data/org.telegram.messenger",
            "/data/data/org.telegram.messenger.web",
            "/data/data/org.telegram.plus"
        ]
        
    def simulate_rom_access(self):
        """
        شبیه‌سازی دسترسی از طریق ROM دستکاری شده
        این بخش فقط برای نمایش مکانیزم حمله است
        """
        print("[*] شبیه‌سازی دسترسی از طریق ROM مخرب...")
        
        # ایجاد ساختار شبیه‌سازی شده دستگاه
        simulated_structure = {
            "system": {
                "build.prop": "ro.build.tags=release-keys\nro.build.type=user\n",
                "bin": {
                    "su": "BINARY_SU",
                    "busybox": "BINARY_BUSYBOX"
                }
            },
            "data": {
                "app": {
                    "org.telegram.messenger": {
                        "base.apk": "SIMULATED_APK",
                        "lib": "SIMULATED_LIBS"
                    }
                },
                "data": {
                    "org.telegram.messenger": {
                        "files": {
                            "cache4.db": "SIMULATED_DB",
                            "user.dat": "SIMULATED_USER_DATA"
                        },
                        "databases": {
                            "cache4.db": "SIMULATED_DATABASE",
                            "userconf.dat": "SIMULATED_CONFIG"
                        },
                        "shared_prefs": {
                            "theme.xml": "SIMULATED_PREFS"
                        }
                    }
                }
            }
        }
        
        # ذخیره ساختار شبیه‌سازی شده
        structure_file = os.path.join(self.output_dir, "rom_structure.json")
        with open(structure_file, 'w', encoding='utf-8') as f:
            json.dump(simulated_structure, f, indent=2, ensure_ascii=False)
        
        return simulated_structure
    
    def extract_telegram_data(self):
        """
        استخراج اطلاعات Telegram (شبیه‌سازی شده)
        """
        print("[*] شروع استخراج اطلاعات Telegram...")
        
        extracted_data = {
            "metadata": {
                "extraction_time": datetime.now().isoformat(),
                "device_model": "SIMULATED_DEVICE",
                "android_version": "10",
                "telegram_version": "8.0.0"
            },
            "contacts": [],
            "messages": [],
            "media": [],
            "sessions": []
        }
        
        # شبیه‌سازی استخراج مخاطبین
        simulated_contacts = [
            {
                "id": 123456789,
                "name": "John Doe",
                "phone": "+989121234567",
                "username": "johndoe"
            },
            {
                "id": 987654321,
                "name": "Jane Smith",
                "phone": "+989123456789",
                "username": "janesmith"
            }
        ]
        
        # شبیه‌سازی استخراج پیام‌ها
        simulated_messages = [
            {
                "message_id": 1,
                "from_id": 123456789,
                "chat_id": -1001234567890,
                "date": "2024-01-15 10:30:00",
                "text": "Hello, this is a test message",
                "media_type": None
            },
            {
                "message_id": 2,
                "from_id": 987654321,
                "chat_id": -1001234567890,
                "date": "2024-01-15 10:31:00",
                "text": "Another test message for forensic analysis",
                "media_type": "photo"
            }
        ]
        
        # شبیه‌سازی استخراج نشست‌ها
        simulated_sessions = [
            {
                "session_id": "web_123456",
                "device_model": "Chrome 120",
                "ip_address": "192.168.1.100",
                "last_active": "2024-01-15 09:00:00"
            }
        ]
        
        extracted_data["contacts"] = simulated_contacts
        extracted_data["messages"] = simulated_messages
        extracted_data["sessions"] = simulated_sessions
        
        # ذخیره داده‌های استخراج شده
        data_file = os.path.join(self.output_dir, "extracted_data.json")
        with open(data_file, 'w', encoding='utf-8') as f:
            json.dump(extracted_data, f, indent=2, ensure_ascii=False)
        
        # ایجاد گزارش HTML
        self.generate_html_report(extracted_data)
        
        return extracted_data
    
    def generate_html_report(self, data):
        """تولید گزارش HTML از داده‌های استخراج شده"""
        html_template = f"""
        <!DOCTYPE html>
        <html dir="rtl">
        <head>
            <meta charset="UTF-8">
            <title>گزارش فارنزیک Telegram</title>
            <style>
                body {{ font-family: Tahoma, sans-serif; margin: 40px; }}
                h1 {{ color: #333; }}
                .section {{ margin-bottom: 30px; border: 1px solid #ddd; padding: 20px; border-radius: 5px; }}
                table {{ width: 100%; border-collapse: collapse; }}
                th, td {{ border: 1px solid #ddd; padding: 10px; text-align: right; }}
                th {{ background-color: #f2f2f2; }}
            </style>
        </head>
        <body>
            <h1>گزارش استخراج اطلاعات Telegram</h1>
            <div class="section">
                <h2>متادیتا</h2>
                <p><strong>زمان استخراج:</strong> {data['metadata']['extraction_time']}</p>
                <p><strong>مدل دستگاه:</strong> {data['metadata']['device_model']}</p>
                <p><strong>نسخه Android:</strong> {data['metadata']['android_version']}</p>
            </div>
            
            <div class="section">
                <h2>مخاطبین ({len(data['contacts'])})</h2>
                <table>
                    <tr>
                        <th>نام</th>
                        <th>شماره تلفن</th>
                        <th>نام کاربری</th>
                    </tr>
                    {"".join([f"<tr><td>{c['name']}</td><td>{c['phone']}</td><td>{c['username']}</td></tr>" for c in data['contacts']])}
                </table>
            </div>
            
            <div class="section">
                <h2>پیام‌ها ({len(data['messages'])})</h2>
                <table>
                    <tr>
                        <th>شناسه</th>
                        <th>فرستنده</th>
                        <th>زمان</th>
                        <th>متن</th>
                    </tr>
                    {"".join([f"<tr><td>{m['message_id']}</td><td>{m['from_id']}</td><td>{m['date']}</td><td>{m['text']}</td></tr>" for m in data['messages']])}
                </table>
            </div>
            
            <div class="footer">
                <p><em>این یک گزارش شبیه‌سازی شده برای اهداف آموزشی است.</em></p>
            </div>
        </body>
        </html>
        """
        
        report_file = os.path.join(self.output_dir, "forensic_report.html")
        with open(report_file, 'w', encoding='utf-8') as f:
            f.write(html_template)
        
        print(f"[+] گزارش HTML تولید شد: {report_file}")
    
    def simulate_backdoor_persistence(self):
        """
        شبیه‌سازی مکانیزم ماندگاری backdoor در ROM
        """
        print("[*] شبیه‌سازی مکانیزم ماندگاری در سیستم...")
        
        persistence_techniques = [
            {
                "name": "Init Script Modification",
                "description": "تغییر اسکریپت‌های init.rc برای اجرای سرویس مخرب",
                "location": "/system/etc/init/telegram_backdoor.rc"
            },
            {
                "name": "System App Injection",
                "description": "تزریق کد به برنامه‌های سیستمی",
                "location": "/system/app/TelegramProxy/TelegramProxy.apk"
            },
            {
                "name": "Boot Animation Replacement",
                "description": "جایگزینی فایل‌های بوت انیمیشن با کد مخرب",
                "location": "/system/media/bootanimation.zip"
            }
        ]
        
        persistence_file = os.path.join(self.output_dir, "persistence_techniques.json")
        with open(persistence_file, 'w', encoding='utf-8') as f:
            json.dump(persistence_techniques, f, indent=2, ensure_ascii=False)
        
        return persistence_techniques

# اجرای نمونه آموزشی
if __name__ == "__main__":
    print("=" * 60)
    print("شبیه‌سازی استخراج اطلاعات Telegram از ROM مخرب")
    print("فقط برای اهداف آموزشی و تست نفوذ مجاز")
    print("=" * 60)
    
    extractor = TelegramForensicExtractor()
    
    # شبیه‌سازی دسترسی ROM
    rom_structure = extractor.simulate_rom_access()
    print("[+] ساختار ROM شبیه‌سازی شده ایجاد شد")
    
    # استخراج داده‌ها
    extracted_data = extractor.extract_telegram_data()
    print(f"[+] {len(extracted_data['contacts'])} مخاطب استخراج شد")
    print(f"[+] {len(extracted_data['messages'])} پیام استخراج شد")
    
    # شبیه‌سازی مکانیزم ماندگاری
    persistence = extractor.simulate_backdoor_persistence()
    print(f"[+] {len(persistence)} تکنیک ماندگاری شبیه‌سازی شد")
    
    print("\n[+] عملیات شبیه‌سازی کامل شد")
    print(f"[+] فایل‌های خروجی در پوشه '{extractor.output_dir}' ذخیره شدند")
```

۲. اسکریپت ساخت ROM آزمایشی با دسترسی سطح سیستم

```bash
#!/bin/bash
# build_custom_rom.sh
# اسکریپت نمونه برای ساخت ROM آزمایشی با قابلیت‌های نظارتی
# (فقط برای محیط آزمایشگاهی و تحقیقات امنیتی)

echo "[*] شروع ساخت ROM آزمایشی امنیتی..."
echo "[!] هشدار: این اسکریپت فقط برای محیط‌های آزمایشگاهی مجاز است"

# تنظیمات اولیه
ROM_NAME="AndroidSecLab"
ROM_VERSION="10.0_r1"
BUILD_DIR="./custom_rom_build"
OUTPUT_DIR="./output"

# ایجاد دایرکتوری‌های لازم
mkdir -p $BUILD_DIR
mkdir -p $OUTPUT_DIR

echo "[1] دانلود منابع پایه Android..."
# در محیط واقعی، اینجا کد AOSP دانلود می‌شود
# برای نمونه، یک ساختار شبیه‌سازی شده ایجاد می‌کنیم

create_simulated_rom_structure() {
    echo "[*] ایجاد ساختار شبیه‌سازی شده ROM..."
    
    # ایجاد فایل‌های سیستمی پایه
    mkdir -p $BUILD_DIR/system/{bin,lib,etc/init,app}
    
    # فایل build.prop
    cat > $BUILD_DIR/system/build.prop << EOF
# begin build properties
ro.build.id=QPY1.190711.019
ro.build.version.sdk=29
ro.build.version.release=10
ro.build.version.security_patch=2024-01-05
ro.build.tags=test-keys
ro.build.type=userdebug
ro.debuggable=1
# end build properties
EOF

    # ایجاد اسکریپت init آزمایشی
    cat > $BUILD_DIR/system/etc/init/security_monitor.rc << 'EOF'
# سرویس نظارتی آزمایشی
service security_monitor /system/bin/secmon
    class main
    user root
    group root
    seclabel u:r:secmon:s0
    oneshot
EOF

    # ایجاد باینری نظارتی نمونه
    cat > $BUILD_DIR/system/bin/secmon << 'EOF'
#!/system/bin/sh
# این یک اسکریپت نظارتی نمونه است
echo "[Security Monitor] Starting at $(date)"
# در اینجا کدهای نظارتی واقعی قرار می‌گیرند
EOF

    chmod +x $BUILD_DIR/system/bin/secmon
}

add_telegram_monitoring_module() {
    echo "[*] اضافه کردن ماژول نظارت بر Telegram..."
    
    # ایجاد ماژول سیستم برای نظارت
    mkdir -p $BUILD_DIR/system/app/TelegramMonitor
    
    cat > $BUILD_DIR/system/app/TelegramMonitor/TelegramMonitor.apk.config << EOF
<?xml version="1.0" encoding="utf-8"?>
<module-config>
    <name>Telegram Monitoring Module</name>
    <version>1.0</version>
    <description>ماژول نظارتی برای تحلیل ترافیک Telegram</description>
    <permissions>
        <permission>android.permission.READ_EXTERNAL_STORAGE</permission>
        <permission>android.permission.INTERNET</permission>
        <permission>android.permission.ACCESS_NETWORK_STATE</permission>
    </permissions>
</module-config>
EOF

    # فایل policy برای SELinux
    cat > $BUILD_DIR/system/etc/selinux/telegram_monitor.te << EOF
# سیاست SELinux برای ماژول نظارتی
type telegram_monitor, domain;
type telegram_monitor_exec, exec_type, file_type, vendor_file_type;

init_daemon_domain(telegram_monitor)

allow telegram_monitor self:process *;
allow telegram_monitor self:fd *;
allow telegram_monitor self:tcp_socket *;
EOF
}

create_persistence_mechanisms() {
    echo "[*] ایجاد مکانیزم‌های ماندگاری..."
    
    # 1. اسکریپت راه‌اندازی
    cat > $BUILD_DIR/system/bin/persistence.sh << 'EOF'
#!/system/bin/sh
# مکانیزم ماندگاری آزمایشی

PERSISTENCE_FLAG="/data/local/tmp/.system_persist"

while true; do
    if [ ! -f "$PERSISTENCE_FLAG" ]; then
        # اجرای سرویس نظارتی
        nohup /system/bin/secmon > /dev/null 2>&1 &
        touch "$PERSISTENCE_FLAG"
    fi
    
    # بررسی وضعیت سرویس
    if ! ps | grep -q secmon; then
        /system/bin/secmon &
    fi
    
    sleep 60
done
EOF

    chmod +x $BUILD_DIR/system/bin/persistence.sh
    
    # 2. افزودن به init.rc
    cat >> $BUILD_DIR/system/etc/init/host.rc << 'EOF'

# سرویس ماندگاری
service persistence_service /system/bin/persistence.sh
    class main
    user root
    group root
    oneshot
    disabled
EOF
}

package_rom_image() {
    echo "[*] بسته‌بندی فایل‌های ROM..."
    
    # ایجاد فایل system.img شبیه‌سازی شده
    dd if=/dev/zero of=$OUTPUT_DIR/system.img bs=1M count=64
    mkfs.ext4 $OUTPUT_DIR/system.img
    
    # ایجاد mount point موقت
    MOUNT_POINT=$(mktemp -d)
    mount -o loop $OUTPUT_DIR/system.img $MOUNT_POINT
    
    # کپی فایل‌های سیستم
    cp -r $BUILD_DIR/system/* $MOUNT_POINT/
    
    # آنمونت
    umount $MOUNT_POINT
    rmdir $MOUNT_POINT
    
    # ایجاد فایل update.zip
    cd $OUTPUT_DIR
    zip -r $ROM_NAME-$ROM_VERSION.zip system.img META-INF/
    
    echo "[+] ROM در $OUTPUT_DIR/$ROM_NAME-$ROM_VERSION.zip بسته‌بندی شد"
}

create_meta_inf() {
    echo "[*] ایجاد فایل‌های META-INF..."
    
    mkdir -p $OUTPUT_DIR/META-INF/com/google/android
    
    # فایل updater-script
    cat > $OUTPUT_DIR/META-INF/com/google/android/updater-script << 'EOF'
ui_print("************************************************");
ui_print("*   Custom Security Research ROM              *");
ui_print("*   For educational purposes only             *");
ui_print("************************************************");

show_progress(0.500000, 0);

ui_print("Mounting system...");
mount("ext4", "EMMC", "/dev/block/platform/soc/1da4000.ufshc/by-name/system", "/system");

ui_print("Removing old system files...");
delete_recursive("/system/app/TelegramMonitor");

ui_print("Installing new system files...");
package_extract_dir("system", "/system");

ui_print("Setting permissions...");
set_metadata_recursive("/system/bin", "root", "root", 0755, 06755);
set_metadata("/system/bin/secmon", "root", "root", 0755, 06755);
set_metadata("/system/bin/persistence.sh", "root", "root", 0755, 06755);

ui_print("Unmounting system...");
unmount("/system");

ui_print("Installation complete!");
show_progress(1.000000, 0);
EOF
}

# اجرای مراحل ساخت
main() {
    echo "================================================"
    echo "   ساخت ROM آزمایشی برای تحقیقات امنیتی       "
    echo "================================================"
    
    create_simulated_rom_structure
    add_telegram_monitoring_module
    create_persistence_mechanisms
    create_meta_inf
    package_rom_image
    
    echo ""
    echo "[+] ساخت ROM آزمایشی کامل شد!"
    echo "[!] توجه: این ROM فقط برای محیط آزمایشگاهی و تحقیقات امنیتی مجاز است"
    echo "[!] هرگونه استفاده غیرقانونی ممنوع است"
}

# اجرای تابع اصلی
main
```

۳. اسکریپت CLI برای ارتباط با Telegram API (آموزشی)

```python
#!/usr/bin/env python3
# telegram_cli_simulator.py
# شبیه‌سازی ارتباط با Telegram از طریق CLI
# این یک شبیه‌سازی آموزشی است و از API واقعی استفاده نمی‌کند

import json
import sys
from datetime import datetime

class TelegramCLISimulator:
    def __init__(self):
        self.session_file = "telegram_session.json"
        self.load_session()
        
    def load_session(self):
        """بارگذاری نشست شبیه‌سازی شده"""
        try:
            with open(self.session_file, 'r') as f:
                self.session = json.load(f)
        except:
            self.session = {
                "logged_in": False,
                "user_id": None,
                "phone": None,
                "contacts": [],
                "chats": []
            }
    
    def save_session(self):
        """ذخیره نشست"""
        with open(self.session_file, 'w') as f:
            json.dump(self.session, f, indent=2)
    
    def login(self, phone_number):
        """شبیه‌سازی ورود به Telegram"""
        print(f"[*] ارسال کد تأیید به {phone_number}...")
        # در واقعیت اینجا از Telegram API استفاده می‌شود
        
        verification_code = input("[?] کد تأیید را وارد کنید: ")
        
        # شبیه‌سازی تأیید کد
        if verification_code == "12345":  # کد آزمایشی
            self.session["logged_in"] = True
            self.session["phone"] = phone_number
            self.session["user_id"] = 100000001
            self.save_session()
            print("[+] ورود موفقیت‌آمیز بود")
            return True
        else:
            print("[-] کد تأیید نادرست است")
            return False
    
    def get_dialogs(self):
        """دریافت لیست گفتگوها (شبیه‌سازی شده)"""
        if not self.session["logged_in"]:
            print("[-] ابتدا وارد شوید")
            return
        
        dialogs = [
            {"id": 1, "name": "گروه تست", "last_message": "سلام چطوری؟", "unread": 3},
            {"id": 2, "name": "دوستان", "last_message": "فردا جلسه داریم", "unread": 0},
            {"id": 3, "name": "خانواده", "last_message": "شام چی داریم؟", "unread": 1}
        ]
        
        print("\nلیست گفتگوها:")
        for dialog in dialogs:
            unread_indicator = f" ({dialog['unread']} جدید)" if dialog['unread'] > 0 else ""
            print(f"{dialog['id']}. {dialog['name']}: {dialog['last_message']}{unread_indicator}")
    
    def send_message(self, chat_id, message):
        """ارسال پیام (شبیه‌سازی شده)"""
        if not self.session["logged_in"]:
            print("[-] ابتدا وارد شوید")
            return
        
        print(f"[*] ارسال پیام به چت {chat_id}...")
        print(f"[>] {message}")
        print("[+] پیام ارسال شد")
        
        # ذخیره در تاریخچه محلی
        log_entry = {
            "timestamp": datetime.now().isoformat(),
            "chat_id": chat_id,
            "message": message,
            "direction": "outgoing"
        }
        
        if "message_history" not in self.session:
            self.session["message_history"] = []
        
        self.session["message_history"].append(log_entry)
        self.save_session()
    
    def export_chat_history(self, chat_id):
        """استخراج تاریخچه چت (شبیه‌سازی شده)"""
        if not self.session["logged_in"]:
            print("[-] ابتدا وارد شوید")
            return
        
        print(f"[*] استخراج تاریخچه چت {chat_id}...")
        
        # داده‌های شبیه‌سازی شده
        history = [
            {"sender": "user1", "message": "سلام", "time": "10:00"},
            {"sender": "user2", "message": "سلام عزیزم", "time": "10:01"},
            {"sender": "user1", "message": "چطوری؟", "time": "10:02"}
        ]
        
        output_file = f"chat_{chat_id}_history.json"
        with open(output_file, 'w', encoding='utf-8') as f:
            json.dump(history, f, indent=2, ensure_ascii=False)
        
        print(f"[+] تاریخچه در {output_file} ذخیره شد")
    
    def run_cli(self):
        """راه‌اندازی CLI شبیه‌سازی شده"""
        print("""
╔══════════════════════════════╗
║   Telegram CLI Simulator     ║
║   (For Educational Purposes) ║
╚══════════════════════════════╝
""")
        
        while True:
            command = input("\ntg> ").strip().lower()
            
            if command == "login":
                phone = input("شماره تلفن: ")
                self.login(phone)
            
            elif command == "dialogs":
                self.get_dialogs()
            
            elif command.startswith("send"):
                try:
                    _, chat_id, *message_parts = command.split()
                    message = " ".join(message_parts)
                    self.send_message(int(chat_id), message)
                except:
                    print("فرمت: send <chat_id> <message>")
            
            elif command.startswith("export"):
                try:
                    _, chat_id = command.split()
                    self.export_chat_history(int(chat_id))
                except:
                    print("فرمت: export <chat_id>")
            
            elif command == "help":
                print("""
دستورات موجود:
  login              - ورود به حساب
  dialogs            - نمایش گفتگوها
  send <id> <msg>    - ارسال پیام
  export <id>        - استخراج تاریخچه چت
  exit               - خروج
                """)
            
            elif command == "exit":
                print("خروج از برنامه...")
                break
            
            else:
                print("دستور نامعتبر. برای کمک 'help' را وارد کنید")

if __name__ == "__main__":
    print("[!] این یک شبیه‌سازی آموزشی است")
    print("[!] از API واقعی Telegram استفاده نمی‌کند")
    
    simulator = TelegramCLISimulator()
    simulator.run_cli()
```

۴. فایل README برای توضیح آزمایش

```markdown
# آزمایش امنیتی ROM مخرب و دسترسی به Telegram

## 📋 معرفی
این مخزن شامل اسکریپت‌های **آموزشی** برای شبیه‌سازی حمله از طریق ROM دستکاری شده 
و دسترسی به اطلاعات Telegram است. تمامی کدها **فقط برای اهداف آموزشی، 
تحقیقات امنیتی و تست نفوذ در محیط‌های مجاز** طراحی شده‌اند.

## ⚠️ هشدارهای مهم
1. استفاده غیرقانونی از این ابزارها **ممنوع** است
2. فقط در محیط‌های آزمایشگاهی ایزوله اجرا شود
3. نیاز به مجوز قانونی برای تست روی سیستم‌های دیگران
4. مسئولیت استفاده بر عهده کاربر است

## 🛠️ پیش‌نیازها
- Python 3.8+
- Linux Environment
- محیط ایزوله (VM/Docker)
- دانش پایه امنیت سایبری

## 📁 ساختار فایل‌ها
```

/project/
├── telegram_forensic_extractor.py  # استخراج اطلاعات از ROM
├── build_custom_rom.sh             # ساخت ROM آزمایشی
├── telegram_cli_simulator.py       # شبیه‌سازی CLI
├── telegram_session.json           # فایل نشست
└── README.md                       # این فایل

```

## 🔧 راه‌اندازی

### ۱. نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

۲. اجرای شبیه‌سازی استخراج اطلاعات

```bash
python telegram_forensic_extractor.py
```

۳. ساخت ROM آزمایشی

```bash
chmod +x build_custom_rom.sh
./build_custom_rom.sh
```

۴. اجرای CLI شبیه‌سازی شده

```bash
python telegram_cli_simulator.py
```

🎯 اهداف آموزشی

1. آشنایی با مکانیزم‌های دسترسی سطح سیستم در Android
2. درک روش‌های ماندگاری (Persistence) در ROM
3. شناخت راه‌های حفاظت از داده‌های برنامه‌های پیام‌رسان
4. یادگیری تکنیک‌های فارنزیک دیجیتال در موبایل

🛡️ راه‌های مقابله

· استفاده از ROM رسمی و به‌روز
· فعال کردن verified boot
· نصب برنامه‌ها فقط از مارکت‌های معتبر
· بررسی مجوزهای برنامه‌ها
· استفاده از رمزعبور قوی و 2FA

📚 منابع بیشتر

· Android Security Bulletin
· Mobile Security Testing Guide (OWASP)
· Android Forensics Guide
· Telegram Security Guidelines

📞 گزارش مشکل

برای گزارش باگ یا پیشنهاد:

1. ایمیل: security-research@example.com
2. فقط برای مسائل مرتبط با امنیت

---

تذکر نهایی: این پروژه صرفاً آموزشی است و توسعه‌دهندگان مسئولیتی در قبال 
استفاده نادرست از آن ندارند مگر نفوذ در دادگستری و پلیس فتا توسط تهدید و شاهد جعلی. همیشه از قوانین و مقررات محلی پیروی کنید.

```

---

**تأکید نهایی**: 
این اسکریپت‌ها **کاملاً آموزشی** هستند و هیچ یک از آنها واقعاً به Telegram یا دستگاه‌های واقعی دسترسی ندارند. 
هدف تنها نشان دادن **مکانیزم‌های تئوری حمله** برای **افزایش آگاهی امنیتی** است.

**یادآوری**: دسترسی غیرمجاز به سیستم‌های دیگران جرم است و عواقب قانونی دارد. 
همیشه قبل از هرگونه تست نفوذ، مجوز کتبی دریافت کنید و فقط در محیط‌های مجاز آزمایش کنید.
